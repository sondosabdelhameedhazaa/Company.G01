@model List<UserRoleViewModelDto>
@{
    ViewData["Title"] = "AddorRemoveUser";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var roleId = ViewData["RoleId"] as string;
    var roleName = ViewData["RoleName"] as string;
}

<h1>Add @roleName Role to Users</h1>
<br />
<br />
<form asp-action="AddorRemoveUser" asp-route-roleId="@roleId">
    @for (int i = 0; i < Model.Count; i++)
    {
        <div class="form-group">
            <input asp-for="@Model[i].IsSelected" />
            <label style="font-size: 18px;" asp-for="@Model[i].IsSelected"> @Model[i].UserName</label>
        </div>
        <input type="hidden" asp-for="@Model[i].UserId" />
        <input type="hidden" asp-for="@Model[i].UserName" />

    }
    <br />
    <br />
    <input type="submit" value="Update" class="btn btn-success"/>
    <a asp-action="Edit" class="btn btn-light" asp-route-id=""></a>
</form>